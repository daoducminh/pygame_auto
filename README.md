# Python Self Driving

## Installation

1. Install Python 3.7.
2. Install Python virtualenv.
3. Create virtual environment for project: `virtualenv .venv`
4. Install required packages: `.venv/bin/python -m pip install .`
